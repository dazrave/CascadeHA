
- name: slack
  platform: slack
  api_key: !secret slack_api
  default_channel: '#alerts'
  # username: 'Home Assistant'

- name: test
  platform: slack
  api_key: !secret slack_api
  default_channel: '#test'
  # username: 'Home Assistant'
